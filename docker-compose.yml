services:
  subtearium:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: subtearium
    ports:
      - "2137:2137"
    volumes:
      - ~/subtearium:/app/data:rw
      - ~/Music:/music:rw
    restart: unless-stopped
