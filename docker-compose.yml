services:
  subtearium:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: subtearium
    ports:
      - "2137:2137"
    volumes:
      - /home/domino/code/docker_dev/subtearium:/app/data:rw
      - /home/domino/Music:/music:rw
    restart: unless-stopped
